<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 
      REQUIRED PERMISSIONS for Karuna Nidhan App
    -->

    <!-- Required for all network requests (API calls, fetching images, etc.) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for react-native-geolocation-service to get the device's precise location -->
    <!-- This is used to find nearby rescue cases. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Required for react-native-image-picker to use the device's camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 
      Required for react-native-image-picker to save images to the gallery and for caching.
      On Android 10 (API 29) and above, this has less impact due to scoped storage, 
      but it is still best practice to include it for older versions and some library functions.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <!-- 
      This tells the Play Store that the app uses the camera, but it is not
      strictly required. This allows users on devices without a camera (like some tablets)
      to still install the app, though camera-related features will not work.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />


    <!-- 
      Your original Application definition
    -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
